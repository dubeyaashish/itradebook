{
  "name": "itradebook",
  "version": "1.0.0",
  "description": "iTradeBook - Complete Trading Management System",
  "main": "server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "deploy": "deploy.bat",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "trading",
    "management",
    "react",
    "nodejs",
    "express"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
